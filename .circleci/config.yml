version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node:8.11
    steps:
      - checkout
      - run: exit 0

workflows:
  test-and-deploy:
    jobs:
      - wait:
          type: approval
      - build:
         requires:
           - wait
